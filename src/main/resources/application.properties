spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://yithu:vithu@cluster0.4q4ztau.mongodb.net/?retryWrites=true&w=majority}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:Dev}
server.port=${PORT:8080}

spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# JWT Configuration
jwt.secret=${JWT_SECRET:change-this-secret-in-production-must-be-at-least-64-characters-long-for-hs512}

# CORS Configuration (comma-separated list of allowed origins)
# Set CORS_ALLOWED_ORIGINS env var in production, e.g: https://your-app.vercel.app
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
